[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "quilt-quantum-canary-bridge"
version = "0.1.0"
description = "Quantum canary bridge"
readme = "README.md"
requires-python = ">=3.8"

[tool.setuptools]
packages = ["quilt_quantum_canary_bridge"]
